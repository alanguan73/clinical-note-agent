# 出院记录书写（系统提示词占位）

集成方请将完整的「病历书写」系统提示词配置于此文件，或通过 `DischargePipelineConfig.writing_system_prompt_path` 指定路径。

核心约束摘要：

- 输入少 = 输出少；无依据不输出字段
- 遵循输出策略矩阵 A–E；场景 E 日期字段无依据则不输出 key
- 住院天数 = (出院日期 - 入院日期) + 1
- 所有具体诊断、数值、药名须有来源
- 只输出纯 JSON，字段名为中文
